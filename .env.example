# Shared team password for the server (basic auth). Leave empty to disable auth.
MAILMAN_PASSWORD=
# Port for the team server
PORT=4000
# SQLite file location (defaults to ./data/mailman.db)
# MAILMAN_DB=/data/mailman.db
